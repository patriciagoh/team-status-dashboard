# Supply-chain hardening: never run dependency lifecycle scripts (postinstall, etc.)
# on install. The design-system guardrail is invoked explicitly via `node ...` in
# package.json, not a lifecycle hook, so it is unaffected.
ignore-scripts=true
# Install exact versions when adding new deps (we pin runtime deps explicitly).
save-exact=true
